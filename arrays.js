var arrLength = arr.map(function(item) {
	return item.length
})

function f(X) {
	alert( arguments.length ? 1 : 0 )
}

function sum() {
	var sum = 0; 
	for (var i = 0; i < arguments.length; i++) {
		sum += arguments[i;
	}
	return sum;
}